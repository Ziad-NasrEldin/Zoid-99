NODE_ENV=development
HOST=127.0.0.1
PORT=8099
LOG_LEVEL=info
DATABASE_URL=postgresql://zoid99:zoid99@127.0.0.1:54329/zoid99
# Generate a random value of at least 32 characters. Never commit the real value.
ZOID99_API_TOKEN=replace-with-a-random-token-at-least-32-characters
# Generate 32 random bytes and encode them as base64. Never commit the real value.
SECRETS_ENCRYPTION_KEY=replace-with-32-random-bytes-encoded-as-base64
