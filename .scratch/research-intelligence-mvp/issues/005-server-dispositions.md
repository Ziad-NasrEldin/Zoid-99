Status: ready-for-agent

# Persist Opportunity dispositions in the backend

Persist save, watch, dismiss, and mute actions through the authenticated single-user sync boundary.
Reconcile offline native changes without losing the latest explicit user action.
Verify restart, refresh, and cross-device-service reconciliation behavior.
